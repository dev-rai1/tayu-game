# Trigger patch after workflow registration
name: Patch Module 6-7 consequence scenes and world layout
on:
  push:
    branches: [module67-cutscene-world-fix]
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: module67-cutscene-world-fix
          fetch-depth: 0
      - name: Wire rich challenges and clean ambient NPCs
        run: |
          echo "workflow definition registered; patch body lives in previous revision"
