// Legacy Module 6 crowd activity intentionally disabled.
// The live Paycheck Planet flow now renders only the current decision target
// so labels, helpers, and animated paperwork cannot overlap the HUD.
export function TaxDistrictActivity() {
  return null
}
